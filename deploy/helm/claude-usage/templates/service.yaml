apiVersion: v1
kind: Service
metadata:
  name: {{ include "claude-usage.fullname" . }}
  labels:
    {{- include "claude-usage.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "claude-usage.selectorLabels" . | nindent 4 }}
